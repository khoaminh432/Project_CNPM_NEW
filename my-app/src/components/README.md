## Cấu trúc thư mục

MapTracking/
├── MapComponent.js          # Component chính
├── handleSearch.js          # Xử lý tìm kiếm
├── handleAnimation.js       # Xử lý di chuyển xe
├── handleDateTime.js        # Xử lý ngày giờ
├── handleMapSetup.js        # Khởi tạo bản đồ
├── handleMarkers.js         # Quản lý markers
├── handleRouteCalculation.js # Tính toán tuyến đường
└── constants.js             # Hằng số và icon